{{- $fullName := include "gsd-remote.fullname" . -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $fullName }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "gsd-remote.labels" . | nindent 4 }}
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      {{- include "gsd-remote.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "gsd-remote.selectorLabels" . | nindent 8 }}
        gsd/project: {{ .Values.projectName }}
      annotations:
        checksum/secrets: {{ include (print $.Template.BasePath "/secret.yaml") . | sha256sum }}
    spec:
      terminationGracePeriodSeconds: 30
      securityContext:
        fsGroup: 1000
      initContainers:
        - name: init
          image: busybox:1.37
          command:
            - sh
            - -c
            - |
              set -e
              # Fix PVC ownership
              chown -R 1000:1000 /workspace /gsd-home

              # Ensure agent dir exists on the PVC
              mkdir -p /gsd-home/agent

              # Make shared volume accessible
              chmod 777 /shared

              {{- if .Values.githubPAT }}
              # Write git credentials to a shared volume
              echo "https://x-access-token:$(cat /secrets/github-pat)@github.com" > /shared/.git-credentials
              chown 1000:1000 /shared/.git-credentials
              chmod 644 /shared/.git-credentials
              {{- end }}

              {{- if .Values.modelsJSON }}
              # Copy models.json into gsd agent dir on the PVC
              cp /secrets/models.json /gsd-home/agent/models.json
              chown 1000:1000 /gsd-home/agent/models.json
              {{- end }}

              {{- if .Values.projectEnv }}
              # Stage project .env for entrypoint to copy after clone
              cp /secrets/project.env /shared/.project-env
              chown 1000:1000 /shared/.project-env
              chmod 644 /shared/.project-env
              {{- end }}

              echo "init complete"
          volumeMounts:
            - name: workspace
              mountPath: /workspace
            - name: gsd-home
              mountPath: /gsd-home
            - name: secrets
              mountPath: /secrets
              readOnly: true
            - name: shared
              mountPath: /shared
      containers:
        - name: gsd
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            - name: WORKSPACE
              value: "/workspace/project"
            - name: GIT_REPO
              value: {{ .Values.gitRepo | quote }}
            {{- if .Values.gitBranch }}
            - name: GIT_BRANCH
              value: {{ .Values.gitBranch | quote }}
            {{- end }}
            - name: PROJECT_NAME
              value: {{ .Values.projectName | quote }}
            - name: NODE_ENV
              value: "production"
            - name: TERM
              value: "xterm-256color"
            {{- if .Values.braveAPIKey }}
            - name: BRAVE_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ $fullName }}-secrets
                  key: brave-api-key
            {{- end }}
            {{- range $key, $value := .Values.extraEnv }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
          volumeMounts:
            - name: workspace
              mountPath: /workspace
            - name: gsd-home
              mountPath: /home/gsd/.gsd
            - name: auth
              mountPath: /home/gsd/.gsd/agent/auth.json
              subPath: auth.json
              readOnly: true
            - name: shared
              mountPath: /home/gsd/.shared
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          livenessProbe:
            exec:
              command: ["tmux", "has-session", "-t", "gsd"]
            initialDelaySeconds: 30
            periodSeconds: 30
            failureThreshold: 5
      volumes:
        - name: workspace
          persistentVolumeClaim:
            claimName: {{ $fullName }}-workspace
        - name: gsd-home
          persistentVolumeClaim:
            claimName: {{ $fullName }}-gsd-home
        - name: secrets
          secret:
            secretName: {{ $fullName }}-secrets
        - name: auth
          secret:
            secretName: {{ $fullName }}-secrets
            items:
              - key: auth.json
                path: auth.json
        - name: shared
          emptyDir: {}
